<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunnyweather.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SunnyWeatherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.weather.WeatherActivity"></activity>
        <activity android:name=".init.RegisterActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".init.ForgetPasswordActivity"/>
        <activity android:name=".ui.user.DialogPassword"/>
        <activity android:name=".ui.user.AppShare"/>

        <activity android:name=".init.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>